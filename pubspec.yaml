name: forgeflow
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  fl_chart: ^1.1.1
  flutter_riverpod: ^2.4.9
  get_it: ^9.2.1
  flutter_animate: ^4.5.0
  haptic_feedback: ^0.6.4+3
  flutter_local_notifications: ^20.1.0
  go_router: ^17.1.0
  intl: ^0.20.2
  uuid: ^4.2.1
  path_provider: ^2.1.1
  google_fonts: ^8.0.2
  # revenue_cat: ^5.0.0 # Placeholder; use actual package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.8
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
